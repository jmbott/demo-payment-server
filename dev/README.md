## Development Configuration and Install Scripts

**docker-compose.yml**

A YAML file that defines how Docker containers should behave during dev.

**run.sh**

Script to kick off the web application.

**commands.py**

Setup database during testing
